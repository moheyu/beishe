# 基础镜像
FROM openjdk:8-jdk-alpine

# 设置工作目录
WORKDIR /opt/travel

# 复制构建产物
COPY target/travel-recommendation-1.0.0.jar app.jar

# 创建上传目录
RUN mkdir -p /opt/travel/uploads

# 暴露端口
EXPOSE 8080

# 启动命令
ENTRYPOINT ["java", "-jar", "app.jar"]