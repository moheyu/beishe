<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wit.travel.mapper.ForumReplyMapper">

    <resultMap id="ForumReplyVOMap" type="com.wit.travel.vo.ForumReplyVO">
        <id property="id" column="id"/>
        <result property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="content" column="content"/>
        <result property="username" column="username"/>
        <result property="avatar" column="avatar"/>
        <result property="parentUsername" column="parent_username"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectForumReplyVOList" resultMap="ForumReplyVOMap">
        SELECT
            fr.id,
            fr.post_id,
            fr.user_id,
            fr.parent_id,
            fr.content,
            u.username,
            u.avatar,
            pu.username AS parent_username,
            fr.create_time
        FROM forum_reply fr
        LEFT JOIN user u ON fr.user_id = u.id
        LEFT JOIN forum_reply pru ON fr.parent_id = pru.id
        LEFT JOIN user pu ON pru.user_id = pu.id
        <where>
            fr.deleted = 0
            <if test="query.postId != null">
                AND fr.post_id = #{query.postId}
            </if>
        </where>
        ORDER BY fr.create_time ASC
    </select>

</mapper>
