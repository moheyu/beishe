<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wit.travel.mapper.ViewRecordMapper">

    <resultMap id="ScenicVOMap" type="com.wit.travel.vo.ScenicVO">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="images" column="images" typeHandler="com.wit.travel.handler.StringToListTypeHandler"/>
        <result property="price" column="price"/>
        <result property="score" column="score"/>
        <result property="location" column="location"/>
        <result property="viewCount" column="view_count"/>
        <result property="categoryId" column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="viewTime" column="view_time"/>
    </resultMap>

    <select id="selectScenicVOByUserId" resultMap="ScenicVOMap">
        SELECT
            s.id,
            s.name,
            s.description,
            s.images,
            s.price,
            s.score,
            s.location,
            s.view_count,
            s.category_id,
            sc.name AS category_name,
            s.status,
            s.create_time,
            vr.view_time
        FROM view_record vr
        LEFT JOIN scenic s ON vr.scenic_id = s.id
        LEFT JOIN scenic_category sc ON s.category_id = sc.id
        WHERE vr.user_id = #{userId}
        ORDER BY vr.view_time DESC
        LIMIT 18
    </select>

    <select id="selectTotalViews" resultType="java.lang.Long">
        SELECT COUNT(*) FROM view_record
    </select>

    <select id="selectTotalViewsByUserId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM view_record WHERE user_id = #{userId}
    </select>

    <select id="selectOldestRecordIds" resultType="java.lang.Long">
        SELECT id FROM view_record 
        WHERE user_id = #{userId} 
        ORDER BY view_time ASC 
        LIMIT #{limit}
    </select>

</mapper>
