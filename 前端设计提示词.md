# 个性化旅游推荐平台 - 前端设计提示词

## 一、项目概述

基于Vue3 + Element Plus + Vue Router + Axios + Pinia 构建一个**个性化旅游推荐平台**的前端应用，后端API已完整实现（Spring Boot + MyBatis-Plus + JWT认证 + Redis缓存），需要构建对应的前端界面。

## 二、技术栈要求

- **核心框架**: Vue 3 (Composition API)
- **UI组件库**: Element Plus
- **路由管理**: Vue Router 5
- **状态管理**: Pinia
- **HTTP请求**: Axios
- **构建工具**: Vite
- **图表库**: ECharts (用于统计页面)

## 三、页面规划与路由设计

### 3.1 公共页面（无需登录）

| 路由路径 | 页面名称 | 功能描述 |
|---------|---------|---------|
| `/` | 首页 | 轮播图、推荐景点、推荐路线、公告列表 |
| `/login` | 登录页 | 用户登录（用户名+密码） |
| `/register` | 注册页 | 用户注册 |
| `/scenic/list` | 景点列表 | 分页查询、分类筛选、标签筛选、搜索 |
| `/scenic/:id` | 景点详情 | 景点信息、图片相册、评论、收藏 |
| `/route/list` | 路线列表 | 分页查询、分类筛选、搜索 |
| `/route/:id` | 路线详情 | 路线详情、途经景点、收藏 |
| `/forum` | 论坛首页 | 帖子列表、精华帖、置顶帖 |
| `/forum/:id` | 帖子详情 | 帖子内容、回复列表、发表回复 |
| `/announcements` | 公告列表 | 系统公告、活动通知 |
| `/announcements/:id` | 公告详情 | 公告详细内容 |

### 3.2 用户中心（需登录）

| 路由路径 | 页面名称 | 功能描述 |
|---------|---------|---------|
| `/user/profile` | 个人资料 | 查看/修改个人信息、头像上传 |
| `/user/preferences` | 偏好设置 | 设置旅游偏好（景点类型、预算、季节等） |
| `/user/collections` | 我的收藏 | 收藏的景点/路线列表 |
| `/user/comments` | 我的评论 | 我的评论/回复记录 |
| `/user/view-history` | 浏览历史 | 浏览过的景点/路线记录 |
| `/user/posts` | 我的帖子 | 我发布的论坛帖子 |
| `/user/replies` | 我的回复 | 我在论坛的回复 |

### 3.3 管理后台（需管理员权限）

| 路由路径 | 页面名称 | 功能描述 |
|---------|---------|---------|
| `/admin` | 仪表盘 | 统计面板、数据概览 |
| `/admin/scenic` | 景点管理 | 景点CRUD、审核、推荐等级设置 |
| `/admin/scenic/add` | 新增景点 | 添加景点（名称、描述、价格、图片、分类） |
| `/admin/scenic/:id/edit` | 编辑景点 | 编辑景点信息 |
| `/admin/route` | 路线管理 | 路线CRUD、审核 |
| `/admin/route/add` | 新增路线 | 添加路线（含途经景点关联） |
| `/admin/route/:id/edit` | 编辑路线 | 编辑路线信息 |
| `/admin/category/scenic` | 景点分类管理 | 增删改查景点分类 |
| `/admin/category/route` | 路线分类管理 | 增删改查路线分类 |
| `/admin/tag` | 标签管理 | 景点标签管理 |
| `/admin/forum` | 论坛管理 | 帖子审核、回复审核 |
| `/admin/announcement` | 公告管理 | 发布、编辑、删除公告 |
| `/admin/user` | 用户管理 | 用户列表、权限管理 |
| `/admin/statistics` | 数据统计 | 景点/路线访问量、用户行为分析 |

## 四、路由守卫设计

### 4.1 路由守卫类型

路由守卫实现要求：
1. beforeEach 全局前置守卫
2. 登录状态检查（token是否存在且有效）
3. 权限检查（管理员页面需验证角色）
4. 页面标题动态设置
5. 路由跳转loading效果

### 4.2 守卫规则

| 页面类型 | 访问条件 |
|---------|---------|
| 公共页面 | 无需登录，可直接访问 |
| 用户中心 | 已登录（token有效） |
| 管理后台 | 已登录且角色为 ADMIN 或 ROOT |
| 未授权 | 跳转登录页，记录redirect地址 |

## 五、请求拦截器设计

### 5.1 请求拦截器 (Request Interceptor)

功能要求：
1. 从localStorage或Pinia获取JWT Token
2. 自动在请求头添加 Authorization: Bearer <token>
3. 请求参数序列化（JSON转formData如果是文件上传）
4. 添加请求时间戳（防止缓存）
5. Content-Type自动设置

### 5.2 响应拦截器 (Response Interceptor)

功能要求：
1. 统一处理业务响应（Result<T>结构）
2. 200成功：直接返回data
3. 401未授权：清除token，跳转登录页
4. 403禁止：提示权限不足
5. 404/500等：http错误提示
6. 业务错误：显示后端返回的错误信息
7. 请求日志记录

## 六、状态管理 (Pinia Stores)

### 6.1 用户状态 (useUserStore)

状态：
- token: JWT令牌
- userInfo: {id, username, nickname, avatar, role}
- isLoggedIn: 登录状态

方法：
- login(credentials): 登录
- logout(): 登出
- fetchUserInfo(): 获取用户信息
- updateUserInfo(data): 更新用户信息

### 6.2 偏好设置状态 (usePreferenceStore)

状态：
- preferences: 用户偏好对象

方法：
- fetchPreferences(): 获取偏好
- savePreferences(data): 保存偏好

### 6.3 缓存状态 (useCacheStore)

状态：
- scenicCategories: 景点分类列表
- routeCategories: 路线分类列表
- tags: 标签列表

方法：
- 分类数据缓存与刷新

## 七、API封装 (Axios)

### 7.1 API模块划分

```
src/api/
├── auth.js         # 登录、注册、登出
├── user.js         # 用户相关
├── scenic.js       # 景点相关
├── route.js        # 路线相关
├── forum.js        # 论坛相关
├── announcement.js # 公告相关
├── collection.js   # 收藏相关
├── comment.js      # 评论相关
├── preference.js   # 偏好相关
├── admin.js        # 管理后台API
└── upload.js       # 文件上传
```

### 7.2 统一响应处理

后端返回格式统一为：
```json
{ "code": number, "data": any, "message": string }
```

前端需要：
1. 拦截器统一解析 Result<T>
2. 错误码映射（200成功，其他为错误）
3. 错误提示使用 Element Plus ElMessage

## 八、组件化开发规范

### 8.1 公共组件

```
src/components/
├── layout/
│   ├── AppHeader.vue      # 顶部导航
│   ├── AppFooter.vue      # 底部信息
│   └── AppSidebar.vue    # 侧边栏（管理后台）
├── common/
│   ├── ScenicCard.vue    # 景点卡片
│   ├── RouteCard.vue     # 路线卡片
│   ├── ForumPostCard.vue # 帖子卡片
│   ├── Pagination.vue    # 分页组件
│   ├── ImageUpload.vue   # 图片上传组件
│   └── SearchBar.vue     # 搜索栏
└── admin/
    ├── DataStatistics.vue # 统计卡片
    └── AuditDialog.vue   # 审核对话框
```

### 8.2 页面组件目录结构

```
src/views/
├── Home/           # 首页
├── Auth/           # 登录注册
├── Scenic/         # 景点模块
├── Route/          # 路线模块
├── Forum/          # 论坛模块
├── Announcement/   # 公告模块
├── User/           # 用户中心
└── Admin/          # 管理后台
```

## 九、关键业务逻辑

### 9.1 景点推荐算法展示
- 首页展示推荐景点（根据推荐等级、评分、浏览量）
- 偏好设置页面收集用户偏好
- 景点列表支持多维度筛选（分类、标签、价格、评分）

### 9.2 路线规划
- 路线包含多个途经景点（按天划分）
- 展示路线预算、最佳出行季节、预计天数

### 9.3 论坛互动
- 发帖、回复、点赞
- 帖子关联景点/路线
- 精华帖、置顶帖标识

### 9.4 收藏与历史
- 景点/路线收藏功能
- 自动记录浏览历史

## 十、Element Plus 组件使用建议

| 场景 | 推荐组件 |
|-----|---------|
| 表格列表 | el-table, el-pagination |
| 表单提交 | el-form, el-input, el-select |
| 消息提示 | ElMessage, ElMessageBox |
| 导航菜单 | el-menu, el-breadcrumb |
| 图片展示 | el-image, el-carousel |
| 弹窗 | el-dialog, el-drawer |
| 标签 | el-tag, el-tabs |
| 加载 | el-loading, el-skeleton |

## 十一、后端API对接说明

### 11.1 后端接口基础信息
- 基础URL: http://localhost:8080/api
- 认证方式: JWT Token (Bearer Token)
- 响应格式: { code: number, data: any, message: string }

### 11.2 主要API端点

#### 认证模块
- POST /api/auth/register - 用户注册
- POST /api/auth/login - 用户登录
- POST /api/auth/logout - 用户登出

#### 用户模块
- GET /api/user/info - 获取当前用户信息
- PUT /api/user/update - 更新用户信息
- GET /api/user/list - 获取用户列表（管理员）

#### 景点模块
- GET /api/admin/scenic/list - 景点列表（分页）
- GET /api/admin/scenic/{id} - 景点详情
- POST /api/admin/scenic - 新增景点
- PUT /api/admin/scenic/{id} - 更新景点
- DELETE /api/admin/scenic/{id} - 删除景点
- GET /api/admin/scenic/recommend - 推荐景点
- GET /api/admin/scenic/category/{categoryId} - 分类景点
- PUT /api/admin/scenic/{id}/audit - 审核景点
- PUT /api/admin/scenic/{id}/recommend-level - 设置推荐等级

#### 路线模块
- GET /api/admin/route/list - 路线列表（分页）
- GET /api/admin/route/{id} - 路线详情
- GET /api/admin/route/detail/{id} - 路线详细信息（含途经景点）
- POST /api/admin/route - 新增路线
- PUT /api/admin/route/{id} - 更新路线
- DELETE /api/admin/route/{id} - 删除路线
- GET /api/admin/route/recommend - 推荐路线
- PUT /api/admin/route/{id}/audit - 审核路线

#### 论坛模块
- GET /api/forum/post/list - 帖子列表
- GET /api/forum/post/{id} - 帖子详情
- POST /api/forum/post - 发布帖子
- DELETE /api/forum/post/{id} - 删除帖子
- GET /api/forum/reply/list - 回复列表
- POST /api/forum/reply - 发表评论回复

#### 公告模块
- GET /api/announcement/list - 公告列表
- GET /api/announcement/{id} - 公告详情
- POST /api/admin/announcement - 发布公告
- PUT /api/admin/announcement/{id} - 更新公告
- DELETE /api/admin/announcement/{id} - 删除公告

#### 收藏模块
- GET /api/collection/list - 我的收藏
- POST /api/collection/add - 添加收藏
- DELETE /api/collection/{id} - 取消收藏

#### 评论模块
- GET /api/comment/list - 评论列表
- POST /api/comment/add - 添加评论
- DELETE /api/comment/{id} - 删除评论

#### 偏好设置
- GET /api/preference - 获取偏好
- POST /api/preference - 保存偏好

#### 分类管理
- GET /api/admin/scenic/category/list - 景点分类列表
- POST /api/admin/scenic/category - 新增景点分类
- PUT /api/admin/scenic/category/{id} - 更新景点分类
- DELETE /api/admin/scenic/category/{id} - 删除景点分类

- GET /api/admin/route/category/list - 路线分类列表
- POST /api/admin/route/category - 新增路线分类
- PUT /api/admin/route/category/{id} - 更新路线分类
- DELETE /api/admin/route/category/{id} - 删除路线分类

#### 标签管理
- GET /api/admin/tag/list - 标签列表
- POST /api/admin/tag - 新增标签
- PUT /api/admin/tag/{id} - 更新标签
- DELETE /api/admin/tag/{id} - 删除标签

#### 统计模块
- GET /api/admin/statistics/overview - 统计概览
- GET /api/admin/statistics/scenic - 景点统计
- GET /api/admin/statistics/route - 路线统计

#### 文件上传
- POST /api/upload/image - 上传图片

## 十二、数据库表结构参考

### 主要数据表
1. **announcement** - 公告表
2. **scenic** - 景点表
3. **scenic_category** - 景点分类表
4. **scenic_tag** - 景点标签表
5. **travel_route** - 旅游路线表
6. **route_category** - 路线分类表
7. **route_scenic_relation** - 路线-景点关联表
8. **user** - 用户表
9. **user_collection** - 用户收藏表
10. **user_comment** - 用户评论表
11. **user_preference** - 用户偏好表
12. **forum_post** - 论坛帖子表
13. **forum_reply** - 论坛回复表
14. **view_record** - 浏览记录表

---

生成时间: 2026-03-18
项目: 个性化旅游推荐平台
